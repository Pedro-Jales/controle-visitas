export const BASEURL="10.0.2.2" // points to PC localhost
export const PORT="3000"        // Should be the same as used in json-server

export const URL=`http://${BASEURL}:${PORT}`
export const VISITANTE=`${URL}/visitantes`
export const VISITA=`${URL}/visitas`